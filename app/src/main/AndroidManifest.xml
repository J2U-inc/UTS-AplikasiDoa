<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplikasidoa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AplikasiDoa">
        <activity android:name=".DoaActivity9"></activity>
        <activity android:name=".DoaActivity8" />
        <activity android:name=".DoaActivity7" />
        <activity android:name=".DoaActivity6" />
        <activity android:name=".DoaActivity5" />
        <activity android:name=".DoaActivity4" />
        <activity android:name=".DoaActivity3" />
        <activity android:name=".DoaActivty2" />
        <activity android:name=".DoaActivity" />
        <activity android:name=".MoveActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>